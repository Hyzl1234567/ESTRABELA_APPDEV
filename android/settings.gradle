rootProject.name = "ESTRABELA_APPDEV"
include(":app")

// Include the local React Native Gradle plugin so Gradle can find
// the `com.facebook.react` plugin implementation during configuration.
includeBuild("../node_modules/@react-native/gradle-plugin")
